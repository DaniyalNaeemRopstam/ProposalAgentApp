# Production placeholders — copy to `.env.local` on Vercel or set Project → Environment Variables.
# NEXT_PUBLIC_* is bundled client-side — never API keys.


NEXT_PUBLIC_API_URL=https://your-app.railway.app
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxx

# Rare: only if Next image optimizer must load localhost API assets in tooling
# NEXT_PUBLIC_INCLUDE_LOCALHOST_IMAGES=
